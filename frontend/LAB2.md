# Звіт про виконання лабораторної роботи №2

**Тема:** Рендеринг списків, ключі та передача даних через Props.
---

## 1. Код рендерингу списку (App.tsx)
Використання методу `.map()` для трансформації масиву даних у масив React-компонентів:

```tsx
{postsData.map((post) => (
  <Post
    key={post.id}
    {...post}
  />
))}