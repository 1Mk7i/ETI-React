# Використовуємо Node.js 20 на базі Alpine для мінімального розміру образу
FROM node:20-alpine

# Встановлюємо робочу директорію всередині контейнера
WORKDIR /app

# Копіюємо файли залежностей
# Це робиться окремо для оптимізації кешування шарів Docker
COPY package.json package-lock.json* ./

# Встановлюємо залежності
RUN npm install

# Копіюємо весь інший код проєкту
COPY . .

# Експонуємо порт, на якому працює Vite
EXPOSE 5173

# Запускаємо сервер розробки
# Важливо: --host дозволяє доступ до сервера ззовні контейнера
# -- дозволяє передати аргумент --host безпосередньо у Vite
CMD ["npm", "run", "dev", "--", "--host"]